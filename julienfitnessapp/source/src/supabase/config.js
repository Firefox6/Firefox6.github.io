// Filled during the private FitTrack build/deploy process. These values are not
// shown or editable anywhere in the product UI. Only a publishable key belongs
// in a client build; never place a service-role key here.
export const SUPABASE_CONFIG = {
  url: "",
  publishableKey: ""
};
